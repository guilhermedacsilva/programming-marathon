<% if show_top_menu? && logged_in? %>
<nav id="top-menu" class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid container-max-size">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Programming Marathon</a>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <% if admin? %>
            <li><%= link_to 'Marathons', marathons_path %></li>
            <li><%= link_to 'My account', user_path(current_user) %></li>
            <li><%= link_to 'Users', users_path %></li>
          <% else %>
            <li><%= link_to 'My team', team_path %></li>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'Logout', logout_path, method: :delete %></li>
        </ul>
      </div><!--/.nav-collapse -->
  </div>
</nav>

<div class="space-after-navbar"></div>
<% end %>
